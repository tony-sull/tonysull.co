{
  "$ref": "#/definitions/photos",
  "definitions": {
    "photos": {
      "type": "object",
      "properties": {
        "published": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "string",
                  "format": "date"
                },
                {
                  "type": "integer",
                  "format": "unix-time"
                }
              ]
            },
            {
              "type": "string",
              "format": "date-time",
              "description": "when the entry was published",
              "markdownDescription": "when the entry was published"
            }
          ],
          "description": "when the entry was published",
          "markdownDescription": "when the entry was published"
        },
        "updated": {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "format": "date-time"
                },
                {
                  "type": "string",
                  "format": "date"
                },
                {
                  "type": "integer",
                  "format": "unix-time"
                }
              ]
            },
            {
              "type": "string",
              "format": "date-time",
              "description": "when the entry was updated",
              "markdownDescription": "when the entry was updated"
            }
          ],
          "description": "when the entry was updated",
          "markdownDescription": "when the entry was updated"
        },
        "author": {
          "type": "string",
          "description": "who wrote the entry, optionally embedded as a Card",
          "markdownDescription": "who wrote the entry, optionally embedded as a Card"
        },
        "category": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "entry categories/tags",
          "markdownDescription": "entry categories/tags",
          "default": []
        },
        "name": {
          "type": "string",
          "description": "caption of the photo, often used for figure captions",
          "markdownDescription": "caption of the photo, often used for figure captions"
        },
        "summary": {
          "type": "string",
          "description": "description of the photo, often used for alt text",
          "markdownDescription": "description of the photo, often used for alt text"
        },
        "photo": {
          "type": "string",
          "description": "src URL for the original image file",
          "markdownDescription": "src URL for the original image file"
        },
        "$schema": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "photo"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}