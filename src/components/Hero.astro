---
import Show from "./Show.astro";

export type Props = {
  title: string;
  cta?: {
    text: string;
    href: string;
  };
};

const { title, cta } = Astro.props;
---

<section>
  <h1>{title}</h1>

  <slot />

  <Show when={cta}>
    <a href={cta!.href}>{cta!.text}</a>
  </Show>
</section>
