---
export type Props = {
  title: string
  cta?: {
    text: string
    href: string
  }
}

const { title, cta } = Astro.props
---

<section>
  <h1>{title}</h1>

  <slot />

  {!!cta?.href && <a href={cta!.href}>{cta!.text}</a>}
</section>
